import React from 'react';

const Spinner: React.FC = () => {
  return (
    <div className="inline-block w-20 h-20 border-4 border-blue-300 border-dotted rounded-full animate-spin-slow"></div>
  );
};

export default Spinner;
